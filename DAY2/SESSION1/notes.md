# Function In Java





# Recursion
- A function calls itself
- It uses base case and recursive case
## Types OF Recursion
    1. Direct Recursion
    2. Indirect Recursion
    3. Tree Recursion
    4. Nested Recursion
    5. Head Recursion
    6. Tail Recursion 

# Call Stack
- Used for storing calls of function and returns the value
- Call stack is a memory frame
- push() and pop()

# Class
- class is an blue print of an object
- It defines the properties of the object
- class is an user defined data type
- class is an keyword used create an new class

# Object
- object is an instance of a class. It represents a real-world entity and contains data and behavior

# Encapsulation
- Wrapping the various of data and methods together inside a class and restricting direct access to the data.
- it combines the various data into single unit. 
- The example of encapsulation is class

# variable
it is an container which stores the data
# variables types
    1.class variables-> it is accessed only inside the class
    2.local variables -> the methods which is accessed locally it is accessed only inside the method or function
    3.instances variables->it can be accessed in both inside or outside the class and inside or outside the method
    4. static variables ->

# Polymorphism
- Polymorphism is the ability of an object or method to take multiple forms. In Java, it is achieved through method overloading and method overriding.
- compile time polymorphism - method overloading
- run the polymorphism - method overloading

# HW
- diamond problem in c++ thats why multiple inheritance is eliminated in java

# Encapsulation
- class
- getters and setters 

# inheritance 
- single level
- multilevel
- hybrid
- hierarchial

# constructors
- default
- paramerters
- non parameters 
- copy constructor
- private constructors

# Diamond
- The Diamond Problem occurs in multiple inheritance when a class inherits from two classes that both inherit from the same parent class. The main issue is ambiguity: the child class may not know which version of the parent method to use.

# Indirect recursion
